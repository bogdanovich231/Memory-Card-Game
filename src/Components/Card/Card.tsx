import "./Card.scss";
import iconTest from "../../assets/logo.svg";

const Card = () => {
  return (
    <div className="card">
      <img src={iconTest} alt="test" />
    </div>
  );
};

export default Card;
